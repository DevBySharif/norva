import { notFound } from "next/navigation"; import { ProductEditor } from "@/components/admin/product-editor"; import { getAdminProductById, getProductFormOptions } from "@/features/products/admin-queries";
export default async function EditProductPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const [product, options] = await Promise.all([getAdminProductById(id), getProductFormOptions()]); if (!product) notFound(); return <main className="admin-paper min-h-[calc(100vh-4rem)] px-4 py-7 sm:px-7"><div className="mx-auto max-w-4xl"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#8b5946]">Catalog products</p><h1 className="mt-2 text-3xl font-semibold">Edit product</h1><div className="mt-7"><ProductEditor product={product} {...options}/></div></div></main>; }
