import { ProductEditor } from "@/components/admin/product-editor"; import { getProductFormOptions } from "@/features/products/admin-queries";
export default async function NewProductPage() { const options = await getProductFormOptions(); return <main className="admin-paper min-h-[calc(100vh-4rem)] px-4 py-7 sm:px-7"><div className="mx-auto max-w-4xl"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#8b5946]">Catalog products</p><h1 className="mt-2 text-3xl font-semibold">Create product</h1><p className="mt-2 text-sm text-muted-foreground">Create one purchasable default variant with pricing and inventory.</p><div className="mt-7"><ProductEditor {...options}/></div></div></main>; }
