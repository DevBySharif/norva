import Link from "next/link"; import { requireCustomer } from "@/lib/auth/session"; import { getWishlistProducts } from "@/features/wishlist/queries"; import { ProductCard } from "@/components/store/product-card";
export default async function WishlistPage() { const user = await requireCustomer(); const items = await getWishlistProducts(user.id); return <main className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><h1 className="text-3xl font-semibold">Wishlist</h1>{items.length ? <div className="mt-7 grid grid-cols-2 gap-5 sm:grid-cols-3 lg:grid-cols-4">{items.map(({ product }) => <ProductCard key={product.id} product={product} initialWishlisted/>)}</div> : <div className="mt-8 rounded-xl border bg-card p-8 text-center"><p className="font-medium">Your wishlist is empty</p><Link className="mt-3 inline-block text-sm underline" href="/products">Browse products</Link></div>}</main>; }
