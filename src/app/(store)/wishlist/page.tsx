import Link from "next/link"; import { requireCustomer } from "@/lib/auth/session"; import { getWishlistProducts } from "@/features/wishlist/queries"; import { ProductCard } from "@/components/store/product-card";
export default async function WishlistPage() { const user = await requireCustomer(); const items = await getWishlistProducts(user.id); return <main className="store-container py-10 sm:py-14"><p className="store-eyebrow">Saved items</p><h1 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Wishlist</h1>{items.length ? <div className="mt-8 grid grid-cols-2 gap-x-4 gap-y-8 sm:grid-cols-3 sm:gap-x-6 lg:grid-cols-4">{items.map(({ product }) => <ProductCard key={product.id} product={product} initialWishlisted/>)}</div> : <div className="mt-10 max-w-xl border border-[#d8d0c3]/70 bg-card p-10 text-center"><p className="font-medium">Your wishlist is empty</p><p className="mt-2 text-sm text-muted-foreground">Save pieces you are considering and return to them anytime.</p><Link className="store-primary-button mt-6" href="/products">Browse products</Link></div>}</main>; }
