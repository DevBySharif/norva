import { notFound } from "next/navigation"; import type { Metadata } from "next"; import { getBrandBySlug } from "@/features/brands/queries"; import { getStoreProducts } from "@/features/products/queries"; import { ProductCard } from "@/components/store/product-card"; import { EmptyState } from "@/components/shared/empty-state";
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const b = await getBrandBySlug((await params).slug); return b ? { title: b.seoTitle ?? b.name, description: b.seoDescription ?? b.description ?? undefined, alternates: { canonical: `/brand/${b.slug}` } } : {}; }
export default async function BrandPage({ params }: { params: Promise<{ slug: string }> }) { const b = await getBrandBySlug((await params).slug); if (!b) notFound(); const { products } = await getStoreProducts({ brand: b.slug }); return <section className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><h1 className="text-3xl font-semibold">{b.name}</h1>{b.description && <p className="mt-3 max-w-xl text-muted-foreground">{b.description}</p>}{products.length ? <div className="mt-8 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4">{products.map((p) => <ProductCard key={p.id} product={p}/>)}</div> : <div className="mt-8"><EmptyState title="No products from this brand"/></div>}</section>; }
