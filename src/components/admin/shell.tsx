import Link from "next/link";
import { Bell, Boxes, CircleHelp, LayoutDashboard, Menu, Package, Settings, ShoppingCart, Tag } from "lucide-react";
import { SignOutButton } from "@/components/admin/sign-out-button";
const items = [["Dashboard", "/admin", LayoutDashboard], ["Orders", "/admin/orders", ShoppingCart], ["Products", "/admin/products", Package], ["Categories", "/admin/categories", Tag], ["Brands", "/admin/brands", Boxes], ["Coupons", "/admin/coupons", Tag], ["Reviews", "/admin/reviews", CircleHelp], ["Notifications", "/admin/notifications", Bell], ["Settings", "/admin/settings", Settings]] as const;
type Identity = { name?: string | null; email?: string | null; role: string } | undefined;
export function AdminShell({ children, user }: { children: React.ReactNode; user: Identity }) { return <div className="min-h-screen bg-[#f7f7f5]"><aside className="fixed inset-y-0 hidden w-64 border-r bg-card p-5 lg:block"><Link href="/admin" className="text-lg font-bold">NORVA<span className="text-muted-foreground">/</span> ADMIN</Link><nav className="mt-8 space-y-1">{items.map(([label, href, Icon]) => <Link key={label} href={href} className="flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium text-muted-foreground hover:bg-muted hover:text-foreground"><Icon className="size-4"/>{label}</Link>)}</nav></aside><div className="lg:pl-64"><header className="relative flex h-16 items-center justify-between border-b bg-card px-4 sm:px-7"><details className="group lg:hidden"><summary role="button" className="flex min-h-11 min-w-11 cursor-pointer list-none items-center justify-center rounded-md" aria-label="Open admin navigation"><Menu className="size-5"/></summary><nav aria-label="Admin navigation" className="absolute left-4 top-14 z-50 max-h-[calc(100vh-4.5rem)] w-64 overflow-y-auto rounded-lg border bg-card p-2 shadow-lg sm:left-7">{items.map(([label, href, Icon]) => <Link key={label} href={href} className="flex min-h-11 items-center gap-3 rounded-md px-3 py-2 text-sm font-medium text-muted-foreground hover:bg-muted hover:text-foreground"><Icon className="size-4"/>{label}</Link>)}</nav></details><div className="hidden text-sm text-muted-foreground sm:block">Operations overview</div><div className="ml-auto flex items-center gap-4"><Link href="/admin/notifications" aria-label="Notifications" className="relative"><Bell className="size-5"/></Link>{user && <div className="text-right"><p className="text-sm font-medium">{user.name ?? user.email}</p><p className="text-xs text-muted-foreground">{user.role}</p><SignOutButton/></div>}</div></header>{children}</div></div>; }
