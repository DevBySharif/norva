import { PackageOpen } from "lucide-react";
export function EmptyState({ title = "Nothing here yet", description = "This area will be ready as the store grows." }: { title?: string; description?: string }) { return <section className="grid min-h-48 place-items-center rounded-lg border border-dashed bg-card p-8 text-center"><div><PackageOpen className="mx-auto mb-3 size-7 text-muted-foreground"/><h2 className="font-semibold">{title}</h2><p className="mt-1 max-w-sm text-sm text-muted-foreground">{description}</p></div></section>; }
